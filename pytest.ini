[pytest]
# Zbieramy testy WYŁĄCZNIE z tests/. `scripts/test_icd.py` ma prefiks `test_`, ale jest
# narzędziem diagnostycznym uderzającym do prawdziwego API WHO — nie może trafić do suity.
testpaths = tests
